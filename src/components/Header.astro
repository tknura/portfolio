---
import { navigation } from '../data/site';
---

<header>
  <a class="wordmark" href="#top" aria-label="Back to top">tk<span>®</span></a>
  <span class="header-note">FRONTEND ENGINEER<br />WITH A DESIGN PROBLEM.</span>
  <nav aria-label="Main navigation">
    {navigation.map((item) => <a href={item.href}>{item.label} <sup>{item.index}</sup></a>)}
    <a class="nav-contact" href="#contact">Say hello ↗</a>
  </nav>
</header>
